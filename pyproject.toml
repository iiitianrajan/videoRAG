[project]
name = "videorag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "iiitianrajan", email = "iiitianrajan@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
videorag = "videorag:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
